<!--index.wxml-->
<view>helllo{{name}}</view>
<view>年龄：{{age}}</view>
<view wx:for="{{students}}">{{item.name}}--{{item.age}}</view>
<view>{{count}}</view>
<button size="mini" bindtap="up">+</button>
<button size="mini" bindtap="down">-</button>